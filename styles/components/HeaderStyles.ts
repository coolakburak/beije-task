import { ViewStyle } from "react-native";

export const HeaderStyles = {
  container: {
    flexDirection: "row",
    justifyContent: "space-between",
    padding: 30,
  } as ViewStyle,
  icons: {
    flexDirection: "row",
    justifyContent: "space-between",
    width: 100,
  } as ViewStyle,
};
